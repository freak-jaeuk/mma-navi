# 스크린샷

README·기획서에서 참조하는 실행 화면 캡처를 여기에 둔다.

| 파일 | 내용 |
|---|---|
| `home.png` | 입력 화면(준비 카드 시작) |
| `refuse.png` | 상담 ④ — 위험질문 자동 거부 + 우회로 |

기획서에 넣은 카드 화면 캡처를 위 파일명으로 저장하면 README 상단 '화면' 표에 바로 노출된다.
없어도 `python scripts/run_server.py`로 직접 확인 가능.
