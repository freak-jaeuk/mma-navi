# 비밀 — 절대 커밋 금지
.env
*.key
secrets/

# 파이썬
__pycache__/
*.pyc

# 원시 microdata 캐시만 제외(런타임 불필요, 4MB).
# 데모용 프리빌드 인덱스(teukgi_index.json, cache/teukgi_emb.npz, 로드맵 집계 JSON)는
# 키·GPU 없이 라이브 데모/클론 실행이 되도록 저장소에 포함한다.
data/cache/*.csv
